#include <stdio.h>

void main() {
	
	int x;
	int a;
	int i,b;
	for (i = 0; i < (((0 && a) + (0 && a) + (1 || a) + (1 || a)) ? 1 : 0) * (((a && 0) + (a || 1)) ? 2 : 0); i++)
	{
		if (!((0 && a) + (0 && a) + (1 || a) + (1 || a))) {



			printf("%d\n", 9);
		}
		else {
			printf("%d\n", 99);
			break;

		}
		switch (2)
		{
		case 2:
			printf("%d\n", 2);
			break;
		}


	}
	printf("%d\n", 888888);
	for (i = 0; i < (((0 && a) + (0 && a) + (1 || a) + (1 || a)) ? 1 : 0) * (((a && 0) + (a || 1)) ? 2 : 0); i++)
	{
		if (!((0 && a) + (0 && a) + (1 || a) + (1 || a))) {



			printf("%d\n", 9);
		}
		else {
			printf("%d\n", 99);

		}
		switch (2)
		{
		case 2:
			printf("%d\n", 2);
			break;
		}


	}

	return;
}
